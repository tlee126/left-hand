# Legacy Prototype Archive

## Overview

The canonical application source for LEFT HAND is now entirely located in:
- `app/`
- `components/`
- `data/`
- `lib/`
- `hooks/`
- `public/`

## Archived Historical References

The following files have been archived into `docs/legacy-reference/`:
- `docs/legacy-reference/index.html`
- `docs/legacy-reference/script.js`
- `docs/legacy-reference/styles.css`

These files represent the original static HTML/CSS/JS prototype. They are preserved for historical reference and context only.

## Build and Runtime Status

- None of the archived files in `docs/legacy-reference/` are imported, required, or executed by the Next.js runtime or build pipeline.
- Production builds and development servers run exclusively through Next.js.

## Pending Items

- `left-hand-next-context.zip` and the root `assets/` directory are intentionally retained and not handled yet pending separate review.
